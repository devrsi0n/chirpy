import * as React from 'react';

// import clsx from 'clsx';

export type IndexProps = {
  children: React.ReactNode;
};

export default function SiteHome(props: IndexProps): JSX.Element {
  return <div>Site home</div>;
}
