query themeOfPage($pageId: uuid!) {
  pageByPk(id: $pageId) {
    id
    url
    project {
      id
      theme
    }
  }
}

query themeOfComment($commentId: uuid!) {
  commentByPk(id: $commentId) {
    id
    page {
      id
      url
      project {
        id
        theme
      }
    }
  }
}
