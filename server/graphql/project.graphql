query themeProjects {
  projects {
    id
  }
}

query themeProjectByPk($id: uuid!) {
  projectByPk(id: $id) {
    id
    name
    theme
  }
}

query projectByPk($id: uuid!) {
  projectByPk(id: $id) {
    id
  }
}
