overwrite: true
schema:
  - ./scripts/graphql-schema.json
documents:
  - 'src/frontend/*.graphql'
generates:
  src/frontend/generated/types.ts:
    plugins:
      - typescript
    config:
      enumsAsTypes: true
  src/frontend:
    preset: near-operation-file
    presetConfig:
      baseTypesPath: ./generated/types.ts
      folder: generated
      extension: .tsx
    plugins:
      - typescript-operations
      - typescript-urql
config:
  withComponents: false
  withHooks: true
  # avoidOptionals: true
  # immutableTypes: true
  # maybeValue: T | null | undefined
  # constEnums: true
  scalars:
    jsonb: 'any'
    timestamptz: string
    uuid: string
hooks:
  afterAllFileWrite:
    - prettier --write
