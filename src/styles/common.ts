import tw from 'twin.macro';

export const bluredBg = tw`bg-white/70 dark:bg-black/70 backdrop-filter backdrop-blur-xl`;

export const bluredOverlay = tw`bg-gray-700 dark:bg-gray-300 bg-opacity-20 backdrop-filter backdrop-blur-sm`;
