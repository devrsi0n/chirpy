import tw from 'twin.macro';

export const bluredBg = tw`bg-white dark:bg-black bg-opacity-80 backdrop-filter backdrop-blur-xl`;

export const bluredOverlay = tw`bg-gray-500 dark:bg-gray-400 bg-opacity-10 backdrop-filter backdrop-blur`;
