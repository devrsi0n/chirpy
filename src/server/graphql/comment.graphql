query comments {
  comments {
    id
  }
}

query siteOwnerByCommentId($commentId: uuid!) {
  commentByPk(id: $commentId) {
    page {
      url
      project {
        userId
      }
    }
    user {
      id
      name
    }
  }
}
