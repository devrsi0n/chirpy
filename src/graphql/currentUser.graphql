# Write your query or mutation here
query currentUser {
  currentUser {
    id
    email
    name
    avatar
    members {
      id
      role
      team {
        id
        name
        projects {
          id
          name
        }
      }
    }
    projects {
      id
      name
    }
  }
}
