# Write your query or mutation here
query currentUser {
  currentUser {
    id
    email
    name
    avatar
    members {
      id
      role
      team {
        id
        name
        project {
          id
          name
        }
      }
    }
    projects {
      id
      name
    }
  }
}
